# Independent repair review and verification follow-up

A separate read-only reviewer checked the combined diff, original-record exporter, Figure 3 provenance chain, attribution, campaign recipes and preservation boundaries. No blocking estimator, figure or attribution defect was found. The reviewer independently ran the preservation and rational-translation checks and inspected the changed Figure 3 image.

One small defect was found in the touched validator path: after one finite sample, an injected NaN coefficient on the second sample could be ignored by Python's `max(errors)`, producing `passes=true` and exit 0. Before merging, the pass predicate was tightened to require every error to be finite. Active tests now exercise true success, a finite discrepancy, a later NaN and a later infinity. These are controlled detector tests, not failures of the actual gate formula or new physical data.

The first repair-wide Figure 1 replay/adoption reads failed with incomplete NPZ files and a fixed-hash discrepancy; a fresh separate retry also contained a truncated NPZ. In each affected file, the immediate post-save hash recorded a complete canonical file, while the end-of-run manifest hashed its later truncated prefix. Thus a scan matching that later manifest did not establish integrity against the fixed expected output. The original failed logs and output directories are retained. A final uninterrupted generation/replay/adoption sequence passed against the unchanged expected hashes and all retained resamples. The cause of the truncation between reads is not established; it is not classified as a numerical/bootstrap defect. See `NPZ_DIAGNOSTIC.md` for the evidence and scope of this conclusion.
