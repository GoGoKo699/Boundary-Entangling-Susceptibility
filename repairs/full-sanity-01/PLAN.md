# Minimal repair implementation

Authorized by the user after the full audit, including merge to `main` only after verification. Repair branch: `fix/full-sanity-01`, starting at audit commit `897dba9926583d12bcaacb214d62a5652c1ba8e1`. Remote `main` is still the audited scientific baseline `00009cf7cc02104e4c776863b3ea551bd38e00f4`; the worktree was clean. The audit branch and its entire directory remain immutable. No manuscript, research extension, licensing/visibility change, or redesign is included.

## Planned implementation and acceptance

1. F-01: preserve the inconsistent compact supporting exports as historical bytes; provide deterministic, manifest-bound replacements from original included records with exact estimand/support/normalization. Validate independently against the audit reconstruction and add active regression tests. Do not blindly copy a consolidated table with a different schema.
2. F-02/F-05b: integrate equation-level entanglement-feature attribution and notation mapping; accurately distinguish established transfer machinery, elementary corollaries and empirical results. Define realignment and limit invariant-classification wording.
3. F-04/F-05: correct only identified provenance, terminology and estimator-label inconsistencies; give executable campaign recipes while distinguishing recovered seeds from unknown historical bootstrap invocations. Change only Figure 3's estimator label; preserve all numerical coordinates and other figures, and retain historical figure identities.
4. F-06: enforce the invariant validator's failure flag with a nonzero exit and promote controlled failure-injection coverage into active tests.
5. F-03: keep an explicit outstanding item for actual editable external schematic sources. Do not fabricate assets or claim an unseen external layout was checked.
6. Execute focused independent repair regressions, all current tests, verification, six-panel reproduction, core-record replay, 5,000-draw reassessment, complete Figure 1 50,000-valid-draw generation/replay/adoption and small simulator/gate checks. Use the existing clean pinned environment; save repair logs outside audit and canonical output paths.
7. Review the combined diff and output/provenance bindings, commit on the repair branch, verify remote CI and pull-request state, then merge using the verified head. Do not bypass protections. Recheck `main`, the unchanged audit reference and the merged tree.

## Resource scope

These are bounded record reconstructions and already documented checks, previously completing in seconds to minutes with one numerical thread. No original large simulation campaign or new bootstrap estimand is authorized here. Preserve the existing scientific observations and the audit's failed assertions as historical evidence; active repaired tests must check the intended behavior independently.

## Deliverable

A repair summary and verification ledger in this directory will identify changed sources, resolved findings, explicit remaining limitations/assets, actual commands and final branch/merge verification. Optional research improvements in the audit fix plan are not part of this implementation.
