# Minimal proposed repair plan

This plan proposes future changes. None of these changes was applied to the pinned scientific baseline. The audit does not authorize manuscript writing, additional broad research, or redesign.

## Blocking scientific defects

No defect overturning the four canonical figures or the core exact identities was found. However, **F-01 blocks trustworthy use of the retained quantitative Checkpoint 04 extensions** and therefore blocks an unqualified repository-complete verdict.

1. Resolve `studies/checkpoint_04/results/intervention_cross_architecture.csv` and `response_stencil_decomposition.csv` against original records. All 30 compact endpoints differ from verified corresponding archive values; ten stencil rows violate their own exact equality. Use audit `results/figure1_compact_table_discrepancies.csv` as the discrepancy inventory, not as an automatic replacement dataset.
2. Choose the smallest adequate route: regenerate a precisely defined export with source-member hashes, row filters, support, normalization and a deterministic generator, or explicitly retire the unreliable compact export and point to the verified archive tables. Preserve historical identities where appropriate. The current documented consolidated generator's decomposition schema is not the same as the compact all-probe export, so a blind file copy is insufficient.
3. Add a regression that binds each retained compact endpoint to source rows and verifies every exact stencil sum. The audit's diagnostic tests supply a minimal failing example. Recheck the affected secondary documentation. Do not change primary figures or overwrite original observations merely to make the tests pass.

Acceptance: every retained compact numeric claim is traceable to one specified estimator and passes independent row/stencil reconstruction within a declared rounding tolerance. If the compact table is retired, all current pages must stop endorsing it as exact/load-bearing. No new physical campaign is necessary for this repair.

## Major attribution/inferential issues

**F-02:** integrate the actual entanglement-feature antecedents at the equations where used: Kuo et al., 1910.11351v2, Eqs.19/60; Akhtar and You, 2006.08797v2, Eq.20. The audit's `LITERATURE.md` gives the dimension, subsystem-toggle, invariant and normalization dictionary. Replace hypothetical equivalent-prior-formula language with confirmed specialization. Credit the stabilizer alphabet and sign corollary as direct deductions. Add brief endpoint/conditioning distinctions for Fan et al.2002.12385 and Rudziński et al.2605.26867. Do not claim a priority guarantee from the bounded search.

The core statistical wording is already substantially restrained. Preserve it: no fresh simulation is required merely because fixed references, changing support, non-simultaneous intervals, model-dependent extrapolation, or selected populations have been disclosed. Any future stronger population or limit claim would require new evidence and separate authorization.

For the older gate-space bootstrap, keep its archived support evidence separate from the adopted Figure 1 bootstrap. The pooled dimension-free purity inequalities and fixed-dimension theorem should not be conflated: the audit verified positive dimension-weighted point margins, but did not regenerate a dimension-weighted full-support extension bootstrap. The minimum adequate current framing is a post-hoc diagnostic with its actual weighting, not a pooled universal-probe confidence guarantee.

## Required asset completion

**F-03:** obtain actual editable sources for whichever external schematic/composite components remain in the agreed figure plan, with input/output mapping and provenance. This is not a request to invent replacement art or reconstitute an unseen Overleaf project. No manuscript TeX/TikZ is required or authorized. The numerical Python panels already reproduce.

## Minor documentation and provenance corrections

- **F-04:** fix claims that a full Checkpoint 04 archive, an original simulation manifest or a preflight-failure log is preserved when the current bundle contains selected members only. Link included members via the manifest. Mark unavailable historical execution records explicitly; do not reconstruct them by guessing. Add exact run-label/base-seed/grid command recipes for future generation; distinguish source defaults from recorded historical invocations and archived-array replay from fresh bootstrap generation.
- **F-05a:** replace Figure 3's derivative-style axis with a label for the within-spectrum coefficient per Δp=.02, consistent with its actual estimator. Keep all plotted numbers and geometry decisions otherwise separate.
- **F-05b:** correct the `response.py` docstring to purity-normalized linear-entropy response; define realignment by indices in THEORY; clarify that the two invariants determine this response rather than completely classify gates.
- **F-05c:** reserve the fixed-side joint-preservation principal-stratum wording for the strict Figure 4 comparison; label the original side-dependent estimator by its actual eligibility rule. Existing detailed conditioning definitions can be linked rather than rewritten wholesale.
- **F-05d:** in transition context distinguish susceptibility from its monitoring coefficient, and complete the truncated Lunt et al. title. Retain the documented contextual crossing-selection limitations.

Acceptance: current quick labels and source-availability statements agree with the precise methods, manifest and executable estimator. Ordinary GitHub Markdown is sufficient; no website, theme or navigation redesign is proposed.

## Minor validation enforcement

**F-06:** make `validate_gate_invariant_formula.py` return nonzero when its `passes` field is false, and retain the audit regression that injects a known coefficient discrepancy without editing source files. Its actual numerical result currently passes. This repair strengthens the gate check; it does not change the physical formula or require a new simulation.

## Known limitations to retain

The historical Figure 1 seed is unrecovered. Current intervals are nominal pointwise, support-conditioned, reference-fixed and post-hoc, with sparse eligible Clifford cells. Figures 3/4 original intervals rely on archived arrays. Independent seeds use the same simulator; support differs between runs and sizes. Extrapolation models do not prove a thermodynamic limit. The far Figure 4 tail is sparse and does not establish a localization length or strict finite range. Licensing and public-release choices remain as stated by the author and were not changed by this audit.

These are not automatic demands for larger campaigns or new estimands. They constrain what can be concluded from the present study.

## Optional improvements, not defects

Only if the author later wants stronger claims: propagate discovery-reference/support-selection uncertainty; obtain more eligible Clifford trajectories; prespecify a common population for stronger size-limit comparisons; run an independently developed full simulator; or publish additional simultaneous inference. Such work broadens the evidence and requires a separate decision. It is not part of repairing the presently retained claims.

## Ordered closeout

First resolve F-01's authoritative supporting tables and F-02's equation attribution. Then complete actual schematic assets and correct availability/terminology statements. Run focused source-to-table and existing core regressions after each authorized repair. Finish by rechecking the claim ledger and completeness status. Do not proceed from this audit directly to manuscript drafting, merging or redesign.
