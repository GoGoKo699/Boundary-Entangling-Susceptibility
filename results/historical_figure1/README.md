# Historical Figure 1 baseline

These are exact copies of the accepted CSV and PDF/PNG/SVG panel before the approved 8 September 2026 uncertainty revision. Their original bootstrap seed and full resample arrays were not recovered. They are preserved provenance, not the current error bars and not reconstructed resamples.

The old complete panel/input hash manifest is `baseline_sha256.json`. The raw source table is also retained unchanged under `accepted_figure1/` inside the root record bundle. See [the adopted recipe](../../docs/FIGURE1_UNCERTAINTY.md) for the current reproducible intervals.
