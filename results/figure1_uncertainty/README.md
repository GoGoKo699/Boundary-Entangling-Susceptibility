# Current Figure 1 uncertainty results

The current panel uses the `eligible_union` rows from `interval_comparison.csv`. `all_confirmatory` is the prespecified sensitivity, not a second set of plotted bars. `cell_support.csv`, `eligibility_patterns.csv`, and `monte_carlo_batches.csv` document the conditioned population and sampling diagnostics.

`expected_replay_sha256.json` identifies all 20 saved resample archives and the numerical tables produced by the reviewed fixed-seed recipe. The full arrays are generated by the documented commands and uploaded by CI, rather than permanently required as another binary Git dataset. All needed source observations and reference spectra are already in the repository's original record bundle.

See [the current procedure and limitations](../../docs/FIGURE1_UNCERTAINTY.md). The historical intervals remain separately identified; no missing historical seed is claimed recovered.
