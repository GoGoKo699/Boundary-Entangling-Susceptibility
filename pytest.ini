[pytest]
# The default environment installs scientific dependencies only.
# Website CI explicitly selects its additional suite after installing
# website/requirements-preview.txt; no tests are skipped in that job.
testpaths = tests
